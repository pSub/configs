#! /usr/bin/env nix-shell
#! nix-shell -i zsh -p stow

# No matter where we are go where the script is.
cd $(dirname $0:A)

# Install all dotfiles with stow.
mkdir $HOME/bin && stow -v bin -t $HOME/bin
stow -v emacs
stow -v git
stow -v misc
stow -v x11
stow -v zsh

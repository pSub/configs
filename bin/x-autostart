#!/bin/sh
# This script is started from ~/.xinitrc and provides
# a host specific autostart for X applications.

emacs --daemon &

case $(hostname) in
  'firmin')
      ;;
  'subiectiva' | 'brauchli')
      ;;
esac
